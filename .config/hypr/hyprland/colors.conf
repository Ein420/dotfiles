# exec = export SLURP_ARGS='-d -c F8DCF1BB -b 57435444 -s 00000000'

general {
    col.active_border = rgba(E2E2E239)
    col.inactive_border = rgba(91919130)
}

misc {
    background_color = rgba(131313FF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true
        bar_text_align = 'left'
        bar_color = rgba(131313EE)
        col.text = rgba(E2E2E2FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
          hyprbars-button = rgba(00000000), 16, 󰖭, hyprctl dispatch killactive
          hyprbars-button = rgba(00000000), 16, , hyprctl dispatch fullscreen 0
        
        
    }
}

windowrulev2 = bordercolor rgba(FDABF6AA) rgba(FDABF677),pinned:1
